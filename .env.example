PORT=3000

# Railway Postgres usually provides this automatically when the service is linked.
DATABASE_URL=postgresql://user:password@host:5432/database

# Local fallback if you are not using DATABASE_URL.
DB_USER=postgres
DB_HOST=localhost
DB_NAME=rubrica3
DB_PASSWORD=password
DB_PORT=5432
